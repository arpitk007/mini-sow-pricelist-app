import React from "react";
import { Typography, Box } from "@mui/material";

const Terms = () => {
    <Box sx={{ textAlign: 'center', mt: 8 }}>
        <Typography variant="h4" >Terms Page</Typography>
        <Typography sx={{ mt: 2}}>Terms and condition will be fetched from DB </Typography>
    </Box>
};

export default Terms;